stage 'Checkout'
    node {
        deleteDir()
        checkout scm extensions: [[$class: 'RelativeTargetDirectory', relativeTargetDir: 'pipeline']]
    }

stage 'Build'
    node {
        script {
            pipeline1.groovy
        }
    }