stage 'Checkout'
    node {
        deleteDir()
        checkout scm
    }

stage 'Build'
    node {
        echo ${WORSKPACE}
        evaluate(new File(${WORSKPACE}+'/pipeline/pipeline1.groovy'))
    }
