pipeline {
    agent any
    stages { 
        stage('Iniciando Construção') {
            steps {
                evaluate(new File("${WORKSPACE}/pipeline/pipeline1.groovy"))
            }
        }
    }
}