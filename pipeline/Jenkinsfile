pipeline {
    agent any
    stages { 
        stage 'Checkout' {
            deleteDir()
            checkout scm
        }

        stage 'Build' {
            Wks = "${WORKSPACE}"
            echo Wks
        }
    }
}